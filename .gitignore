# Build directories
build/
dist/
*.o

# SDK directories (use XPLANE_SDK_DIR environment variable instead)
SDK/
macos-sdk-headers/
macos-headers.tar.gz

# IDE files
.vscode/*.log
.vscode
.idea/
*.swp
*.swo
*~
.cache/
compile_commands.json

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile

# OS files
.DS_Store
Thumbs.db

# Config files (user-specific)
*.dat


# X-Plane logs copied in for debugging. The sim rewrites Log.txt on every
# start, so a copy dropped here is always a transient artefact.
Log.txt
Log_*.txt

# Working notes that belong outside a public repo. Session handoffs live in a
# secret gist; release notes are pasted into the GitHub release by hand.
*-handoff-*.md
RELEASE-NOTES-*.md
